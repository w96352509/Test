package com.ocp.day30;

public class Note {
/*
.sequential()預設單工
.parallel()平行多工
資料越大 處理速度越明顯
    .limit(n)最多顯示n個
   Stream.iterate(0, n -> n+1) (起點,無限增加永遠的+1)
    .peek(x-> System.out.prints(x))//是個Consumer函式(接受一個引質,但沒有回傳質),
     接受每個串流數,但無回傳質
   */    
}
